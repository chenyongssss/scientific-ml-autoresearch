# Advection example

This is a toy example for the v0 workflow.

It does not solve a real PDE. Instead, it mimics a scientific ML benchmark by:

- reading a nested config
- producing deterministic metrics
- writing `metrics.json`

The goal is to demonstrate the workflow, directory structure, and experiment bookkeeping.
